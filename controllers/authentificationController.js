/**
 * Le fichier authentification.js a pour mission de gérer les authentifications de utilisations.
 */

module.exports = {
    // la vue register
    registerView : (req, res) => {
        res.render("register");
    }
}